#include <stdio.h>

int main(int argc, char** argv)
{
	int num = 1;
	int prn = -1;
	int cnt = 0;
	int sum = 0;
	while (num != 0) {
		scanf_s("%d", &num);
		if (num = prn) {
			sum++;
		}
		else {
			sum = 1;
		}
		if (sum > cnt) {
			cnt = sum
		}
	}
	printf("%d", cnt);
	return 0;
}